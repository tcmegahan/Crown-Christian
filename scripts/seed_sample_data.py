#!/usr/bin/env python3
# Minimal seed script to create demo data when Django is configured.
import os
import sys

print("Seed script placeholder — run Django migrations and create data via Django shell.")
