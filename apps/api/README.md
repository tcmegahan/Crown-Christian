API app (Django/DRF) placeholder. Run `django-admin startproject api .` here to create the project in this folder, or use the monorepo's `apps/api` as the project root.

Requirements in `requirements-dev.txt`.
