# Example environment variables for crown2_api
# Copy this to .env and fill values for production

# Security
SECRET_KEY=replace-me-with-a-long-random-secret

# Debug (True/False)
DJANGO_DEBUG=False

# Comma-separated hosts allowed to serve the app
ALLOWED_HOSTS=example.com,www.example.com

# Database (example using sqlite for development)
# For production use a managed DB (Postgres) and set DATABASE_URL accordingly
DATABASE_URL=sqlite:///db.sqlite3

# Static files
STATIC_ROOT=/app/static

# Azure settings (set when deploying to Azure)
# AZURE_WEBAPP_NAME=
# AZURE_WEBAPP_PUBLISH_PROFILE=
